---
description: 实验管理规范 - 日志、结果保存、模型管理
globs: **/*.py
alwaysApply: false
---

# 实验管理规范

## 1. 实验记录

每次实验必须记录以下信息：

```python
experiment_config = {
    'timestamp': '2026-04-13_16-00-00',
    'model_version': 'swin_ca_v1',
    'dataset_version': 'cwru_preprocessed_v2',
    'hyperparameters': {
        'batch_size': 32,
        'learning_rate': 0.001,
        'epochs': 100,
    },
    'random_seed': 42,
}
```

## 2. 日志规范

### 2.1 日志格式

使用结构化日志（JSON格式），便于后续分析：

```python
import json
import logging
from datetime import datetime

class ExperimentLogger:
    """实验日志记录器"""
    
    def __init__(self, log_dir: str, experiment_name: str):
        self.log_dir = Path(log_dir)
        self.log_dir.mkdir(parents=True, exist_ok=True)
        
        timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
        self.log_file = self.log_dir / f"{experiment_name}_{timestamp}.jsonl"
    
    def log(self, metric: str, value: float, step: int = None):
        """记录单个指标"""
        entry = {
            'timestamp': datetime.now().isoformat(),
            'metric': metric,
            'value': value,
            'step': step,
        }
        with open(self.log_file, 'a') as f:
            f.write(json.dumps(entry) + '\n')
    
    def log_dict(self, data: dict):
        """批量记录指标"""
        with open(self.log_file, 'a') as f:
            f.write(json.dumps(data) + '\n')
```

### 2.2 必须记录的指标

| 类别 | 指标 |
|------|------|
| 训练 | loss, learning_rate, gradient_norm |
| 验证 | accuracy, f1_score, confusion_matrix |
| 性能 | training_time, memory_usage |

## 3. 结果保存

### 3.1 文件格式

| 类型 | 格式 | 说明 |
|------|------|------|
| 模型权重 | `.pth` | 包含epoch信息 |
| 指标结果 | `.csv` | 便于后续分析 |
| 可视化 | `.png` (300 DPI) | 高分辨率图表 |
| 实验报告 | `.md` | 在 `06_results/report/` 目录下 |

### 3.2 目录结构

```
06_results/
├── figures/           # 可视化图表
│   ├── cwt_compare.png
│   └── confusion_matrix.png
├── metrics/          # 评估指标
│   ├── accuracy.csv
│   └── f1_scores.csv
└── report/           # 实验报告
    └── experiment_20240307.md
```

### 3.3 模型保存

```python
def save_checkpoint(
    model,
    optimizer,
    epoch: int,
    metrics: dict,
    save_dir: Path
):
    """保存训练检查点"""
    checkpoint = {
        'epoch': epoch,
        'model_state_dict': model.state_dict(),
        'optimizer_state_dict': optimizer.state_dict(),
        'metrics': metrics,
    }
    
    path = save_dir / f"checkpoint_epoch_{epoch}.pth"
    torch.save(checkpoint, path)
    return path
```

## 4. 评价指标

### 4.1 指标定义

| 指标 | 计算 | 用途 |
|------|------|------|
| Accuracy | 正确数/总数 | 整体性能 |
| F1-Score | 2×(P×R)/(P+R) | 平衡考虑 |
| Precision | TP/(TP+FP) | 阳性预测准确性 |
| Recall | TP/(TP+FN) | 故障检出能力 |

### 4.2 计算代码

```python
from sklearn.metrics import (
    accuracy_score,
    f1_score,
    classification_report,
    confusion_matrix
)

def evaluate_model(y_true, y_pred) -> dict:
    """评估模型性能"""
    return {
        'accuracy': accuracy_score(y_true, y_pred),
        'f1_macro': f1_score(y_true, y_pred, average='macro'),
        'f1_weighted': f1_score(y_true, y_pred, average='weighted'),
        'confusion_matrix': confusion_matrix(y_true, y_pred),
        'classification_report': classification_report(y_true, y_pred),
    }
```

## 5. 版本控制

- 重要实验结果打Git标签
- 配置文件版本化管理
- 代码提交前必须通过测试
- 📊 实验结果定期备份

## 6. 验证清单

- [ ] 每次实验有完整配置记录
- [ ] 日志使用结构化格式
- [ ] 模型权重保存完整
- [ ] 评估指标计算正确
- [ ] 结果文件命名规范
