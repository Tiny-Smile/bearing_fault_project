---
description: Python代码规范 - 命名、类型注解、文档字符串
globs: **/*.py
alwaysApply: true
---

# Python 代码规范

## 1. 起名字的规矩（统一使用英文命名）

| 东西 | 怎么起名 | 好例子 | 坏例子 |
|------|----------|--------|--------|
| 变量 | 用小写，单词用下划线连 | `signal_data`, `sample_count` | `signalData`, `样本数` |
| 函数/方法 | 用小写，动词开头，下划线连接 | `load_data()`, `split_train_test()` | `LoadData()`, `加载数据()` |
| 类名 | 每个单词首字母大写（PascalCase） | `CWRUDataset`, `CNN1D` | `data_loader`, `cnn_model` |
| 常量 | 全用大写，下划线连接 | `DEFAULT_SAMPLE_LENGTH`, `MAX_EPOCHS` | `最大样本数`, `SampleRate` |
| 私有东西 | 前面加一个下划线 | `_cache_data`, `_init_weights()` | `privateData`, `__data` |

> **重要**：所有函数名、变量名、参数名统一使用英文命名，不使用中文拼音或中文。中文仅用于注释和文档字符串中解释说明。英文命名遵循 Python 社区惯例（PEP 8）。

### 命名风格对照表

```python
# 变量命名（小写下划线）
signal_data           # 信号数据
sample_length         # 样本长度
train_ratio           # 训练集比例
class_distribution    # 类别分布

# 函数命名（小写下划线，动词开头）
def load_data():           # 加载数据
def split_train_test():    # 划分训练测试集
def normalize_signal():    # 归一化信号
def train_epoch():         # 训练一个epoch

# 类命名（PascalCase）
class CWRUDataset:         # CWRU数据集类
class CNN1D:              # 1D-CNN模型类
class Trainer:            # 训练器类
class Evaluator:          # 评估器类

# 常量命名（全大写下划线）
DEFAULT_SAMPLE_LENGTH = 1024
DEFAULT_STRIDE = 512
MAX_EPOCHS = 100


## 2. 给函数加上类型提示（让代码更好懂）

### 什么是类型提示？
就是告诉别人这个变量应该是什么类型，比如是数字、字符串、还是列表。

```python
from typing import List, Tuple, Optional

# 类型提示格式：
# def 函数名(参数: 参数类型) -> 返回类型:

def calculate_mean(data: List[float]) -> float:
    """计算一串数字的平均值"""
    total = sum(data)
    count = len(data)
    return total / count

# 用法
scores = [90, 85, 78, 92]
avg_score = calculate_mean(scores)
print(f"平均分是: {avg_score}")
```

### 常用类型提示

| 类型 | 写法 | 什么意思 |
|------|------|----------|
| 整数 | `int` | 1, 2, 100 |
| 小数 | `float` | 1.5, 3.14 |
| 字符串 | `str` | "hello" |
| 列表 | `List[类型]` | `List[int]` 表示整数列表 |
| 可选值 | `Optional[类型]` | 可以是某个类型，也可以是 None |
| 多个返回值 | `Tuple[类型1, 类型2]` | 返回两个值 |

### 举例

```python
from typing import List, Tuple, Optional

# 一个输入，一个输出
def read_file(file_path: str) -> List[str]:
    """读取文件，返回每行内容"""
    with open(file_path, 'r', encoding='utf-8') as f:
        lines = f.readlines()
    return lines

# 多个输入，多个输出
def process_data(data: List[float], normalize: bool) -> Tuple[List[float], float]:
    """处理数据，可选标准化，返回处理后的数据和平均值"""
    mean = sum(data) / len(data)
    if normalize:
        data = [x - mean for x in data]
    return data, mean

# 有些参数可以没有（可选参数）
def create_matrix(rows: int, cols: int, fill_value: Optional[float] = None) -> List[List[float]]:
    """创建一个矩阵"""
    if fill_value is None:
        return [[0.0] * cols for _ in range(rows)]
    return [[fill_value] * cols for _ in range(rows)]
```

## 3. 给函数写说明（docstring）

每个函数都要写说明，告诉别人这个函数是干什么的。

### 格式（用中文写）

```python
def add(a: int, b: int) -> int:
    """
    计算两个数字的和

    Args:
        a: 第一个加数
        b: 第二个加数

    Returns:
        两个数字相加的结果

    Example:
        >>> result = add(3, 5)
        >>> print(result)
        8
    """
    return a + b
```

### 说明要包含什么

```
1. 一句话说清楚这个函数干什么
2. 参数是什么（如果有的话）
3. 返回什么（如果有的话）
4. 举例（可选，但推荐写）
```

## 4. 怎么写注释

### ✅ 好的注释（解释为什么这样做）

```python
# 对数尺度：低频部分需要更精细的分析
scales = [1, 2, 4, 8, 16, 32, 64]

# 这里用 0.01 而不是 0.1，后者会导致梯度爆炸
learning_rate = 0.01

# 归一化到 [0, 1]，方便模型处理
data = (data - min_val) / (max_val - min_val)
```

### ❌ 不好的注释（说了等于没说）

```python
# 遍历每个样本
for sample in sample_list:
    process(sample)

# 如果 x 大于 0
if x > 0:
    print("positive")
```

## 5. 错误处理（让程序崩溃时知道哪里出错）

### 基本的 try-except

```python
def read_data(file_path: str):
    """读取数据文件"""
    try:
        with open(file_path, 'r') as f:
            data = f.read()
        print("读取成功!")
        return data
    except FileNotFoundError:
        print(f"错误: 文件不存在 - {file_path}")
    except Exception as e:
        print(f"错误: 读取失败 - {e}")
```

### 主动检查并报错

```python
def process_signal(signal, sampling_rate):
    """处理振动信号"""
    # 先检查输入是否合法
    if sampling_rate <= 0:
        raise ValueError("采样率必须大于0")

    if len(signal) == 0:
        raise ValueError("信号不能为空")

    # 处理逻辑
    processed = signal * 2
    return processed
```

## 6. 代码审查清单

写完代码后，检查一下：

- [ ] 所有函数都有说明（docstring）
- [ ] 所有函数都有类型提示
- [ ] 异常处理写好了
- [ ] 路径没有硬编码
- [ ] 注释解释了为什么这样做
- [ ] 名字起得清楚易懂
