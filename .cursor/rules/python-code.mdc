---
description: Python代码规范 - 命名、类型注解、文档字符串
globs: **/*.py
alwaysApply: true
---

# Python 代码规范

## 1. 起名字的规矩

| 东西 | 怎么起名 | 好例子 | 坏例子 |
|------|----------|--------|--------|
| 变量 | 用小写，单词用下划线连 | `signal_data`, `count` | `signalData`, `countNum` |
| 函数 | 用小写，动词开头 | `加载数据()`, `保存图片()` | `LoadData()`, `GetData()` |
| 类名 | 每个单词首字母大写 | `数据加载器`, `CNN模型` | `data_loader`, `cnn_model` |
| 常量 | 全用大写 | `最大样本数`, `采样率` | `MAX_COUNT`, `SampleRate` |
| 私有东西 | 前面加一个下划线 | `_缓存数据`, `_内部函数()` | `privateData`, `__data` |

## 2. 给函数加上类型提示（让代码更好懂）

### 什么是类型提示？
就是告诉别人这个变量应该是什么类型，比如是数字、字符串、还是列表。

```python
# 导入需要的类型
from typing import List, Tuple, Optional

# 类型提示格式：
# def 函数名(参数: 参数类型) -> 返回类型:

def 计算平均值(数字列表: List[float]) -> float:
    """计算一串数字的平均值"""
    总和 = sum(数字列表)
    个数 = len(数字列表)
    return 总和 / 个数

# 用法
成绩 = [90, 85, 78, 92]
平均分 = 计算平均值(成绩)
print(f"平均分是: {平均分}")
```

### 常用类型提示

| 类型 | 写法 | 什么意思 |
|------|------|----------|
| 整数 | `int` | 1, 2, 100 |
| 小数 | `float` | 1.5, 3.14 |
| 字符串 | `str` | "hello" |
| 列表 | `List[类型]` | `List[int]` 表示整数列表 |
| 可选值 | `Optional[类型]` | 可以是某个类型，也可以是 None |
| 多个返回值 | `Tuple[类型1, 类型2]` | 返回两个值 |

### 举例

```python
from typing import List, Tuple, Optional

# 一个输入，一个输出
def 读取文件(文件路径: str) -> List[str]:
    """读取文件，返回每行内容"""
    with open(文件路径, 'r', encoding='utf-8') as f:
        内容 = f.readlines()
    return 内容

# 多个输入，多个输出
def 处理数据(数据: List[float], 是否标准化: bool) -> Tuple[List[float], float]:
    """处理数据，可选标准化，返回处理后的数据和平均值"""
    平均值 = sum(数据) / len(数据)
    if 是否标准化:
        数据 = [x - 平均值 for x in 数据]
    return 数据, 平均值

# 有些参数可以没有（可选参数）
def 创建矩阵(行数: int, 列数: int, 填充值: Optional[float] = None) -> List[List[float]]:
    """创建一个矩阵"""
    if 填充值 is None:
        return [[0.0] * 列数 for _ in range(行数)]
    return [[填充值] * 列数 for _ in range(行数)]
```

## 3. 给函数写说明（docstring）

每个函数都要写说明，告诉别人这个函数是干什么的。

### 格式（用中文写）

```python
def 加法(数字1: int, 数字2: int) -> int:
    """
    计算两个数字的和
    
    参数:
        数字1: 第一个加数
        数字2: 第二个加数
    
    返回:
        两个数字相加的结果
    
    例子:
        >>> 结果 = 加法(3, 5)
        >>> print(结果)
        8
    """
    return 数字1 + 数字2
```

### 说明要包含什么

```
1. 一句话说清楚这个函数干什么
2. 参数是什么（如果有的话）
3. 返回什么（如果有的话）
4. 举例（可选，但推荐写）
```

## 4. 怎么写注释

### ✅ 好的注释（解释为什么这样做）

```python
# 用对数尺度是因为低频部分我们需要更精细的分析
scales = [1, 2, 4, 8, 16, 32, 64]  # 对数增长

# 这里要用 0.01 而不是 0.1，因为后者会导致梯度爆炸
学习率 = 0.01

# 归一化到 [0, 1]，方便模型处理
数据 = (数据 - 最小值) / (最大值 - 最小值)
```

### ❌ 不好的注释（说了等于没说）

```python
# 遍历每个样本
for 样本 in 样本列表:
    处理(样本)

# 如果 x 大于 0
if x > 0:
    print("正数")
```

## 5. 错误处理（让程序崩溃时知道哪里出错）

### 基本的 try-except

```python
def 读取数据(文件路径: str):
    """读取数据文件"""
    try:
        # 尝试打开文件
        with open(文件路径, 'r') as f:
            数据 = f.read()
        print("读取成功！")
        return 数据
    except FileNotFoundError:
        # 文件不存在
        print(f"错误：文件不存在 - {文件路径}")
    except Exception as e:
        # 其他错误
        print(f"错误：读取失败 - {e}")
```

### 主动检查并报错

```python
def 处理信号(信号, 采样率):
    """处理振动信号"""
    # 先检查输入是否合法
    if 采样率 <= 0:
        raise ValueError("采样率必须大于0")
    
    if len(信号) == 0:
        raise ValueError("信号不能为空")
    
    # 检查通过，开始处理
    处理后的信号 = 信号 * 2  # 这里是举例
    return 处理后的信号
```

## 6. 代码审查清单

写完代码后，检查一下：

- [ ] 所有函数都有说明（docstring）
- [ ] 所有函数都有类型提示
- [ ] 异常处理写好了
- [ ] 路径没有硬编码
- [ ] 注释解释了为什么这样做
- [ ] 名字起得清楚易懂
