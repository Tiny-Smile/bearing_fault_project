---
description: 数据处理规范 - 预处理、增强、格式要求
globs: **/*.py
alwaysApply: false
---

# 数据处理规范

## 1. 数据预处理流程（一步一步来）

处理振动信号的步骤：

```
原始信号 → 去趋势（去漂移） → 滤波（去噪声） → 裁剪（统一长度） → 标准化
```

| 步骤 | 干什么 | 参数 |
|------|--------|------|
| 去趋势 | 把信号的基准线弄直 | - |
| 滤波 | 去掉高频噪声 | 截止频率 1000 Hz |
| 裁剪 | 统一每个样本的长度 | 2048 点 |
| 标准化 | 把数值变成 0 附近 | 平均值0，标准差1 |

## 2. 数据长什么样

### 信号数据格式

```python
# 加载数据
数据 = np.load("数据文件.npz")

# 特征数据（X）：形状是 (样本数, 长度)
# 比如 (100, 2048) 表示有100个样本，每个2048个点
X = 数据['X']

# 标签数据（y）：形状是 (样本数,)
# 0=正常, 1=内圈故障, 2=外圈故障, 3=滚动体故障
y = 数据['y']
```

### 标签含义

| 数字 | 意思 | 英文 |
|------|------|------|
| 0 | 正常，没毛病 | Normal |
| 1 | 内圈有故障 | Inner Race |
| 2 | 外圈有故障 | Outer Race |
| 3 | 滚动体有故障 | Ball |

## 3. 数据集怎么分

把数据分成三份：

```
全部数据
├── 训练集 70%  → 用来训练模型
├── 验证集 15%  → 训练时调整参数
└── 测试集 15%  → 最后评估模型效果
```

### 代码示例

```python
from sklearn.model_selection import train_test_split

# 先分：训练集 vs (验证集+测试集)
X_训练, X_剩余, y_训练, y_剩余 = train_test_split(
    X, y,
    test_size=0.3,      # 30% 拿出来
    random_state=42,    # 随机种子，保证每次分的一样
    stratify=y          # 保证每个类别比例一样
)

# 再分：验证集 vs 测试集
X_验证, X_测试, y_验证, y_测试 = train_test_split(
    X_剩余, y_剩余,
    test_size=0.5,      # 15% 给测试集
    random_state=42,
    stratify=y_剩余
)
```

## 4. 保存和读取数据

### 保存数据

```python
import numpy as np

# 用 np.savez_compressed 保存数据（省空间）
np.savez_compressed(
    "训练数据.npz",    # 文件名
    X=X_训练,          # 特征数据
    y=y_训练           # 标签数据
)
print("保存成功！")
```

### 读取数据

```python
# 读取数据
数据 = np.load("训练数据.npz")

# 取出数据
X = 数据['X']  # 特征
y = 数据['y']  # 标签

print(f"加载了 {len(X)} 个样本")
```

## 5. 注意事项

### ⚠️ 原始数据绝对不能改！

```
02_data/raw/  ← 这里的文件只读不改！
02_data/preprocessed/  ← 预处理后的数据放这里
```

### ⚠️ GAN 生成的数据要检验

用 cDWGAN-GP 生成的数据要检查：
- 时域波形看着像不像真的
- 频域特征有没有失真

## 6. 检查清单

- [ ] 数据形状正确（用 `.shape` 看看）
- [ ] 标签只有 0, 1, 2, 3
- [ ] 训练/验证/测试集比例对
- [ ] 每个类别都有样本
- [ ] 原始数据没有被修改
